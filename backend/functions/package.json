{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "serve": "NODE_ENV=development firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "NODE_ENV=production firebase deploy --only functions",
    "deploy:dev": "NODE_ENV=development firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "@supabase/supabase-js": "^2.50.0",
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  },
  "private": true
}
